#!/usr/bin/env ruby
require './lib/command'


if ARGV.count == 1 && ARGV[0] == "help"
  Command.start(["help", "csv2strings"],{})
else
  args = ["csv2strings"]
  args += ARGV
  Command.start(args,{})
end
